// @flow

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderColor: 'black',
    borderWidth: 1,
    borderRadius: 10,
  },
  iconContainerStyle: {
    height: 32,
    width: 32,
  },
});
