// @flow

import React, { Component } from 'react';
import { Text, View } from 'react-native';

class OrderFooterView extends Component {
  render = () => {
    return (
      <View>
        <Text>Add to order</Text>
      </View>
    );
  };
}

export default OrderFooterView;
